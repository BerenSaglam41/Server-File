# Mac için — kopyala: cp .env.mac .env
# (NFS files-01 sunucusu 192.168.64.3'ten mount edilmiş olmalı)
# Mount: sudo mkdir -p /Volumes/platform-files && sudo mount -t nfs -o resvport 192.168.64.3:/srv/files /Volumes/platform-files
STORAGE_PATH=/Volumes/platform-files
